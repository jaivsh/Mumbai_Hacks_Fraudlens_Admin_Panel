# Marketing site deploy (GCS + Cloud CDN)

This repo includes a static landing site at `marketing_site/`.\n
## 1) Create a public bucket and upload site\n
Pick a globally-unique bucket name (often your domain, e.g. `www.fraudlens.in`).\n
```bash\nPROJECT_ID=\"fraudlens-aura-prod\"\nREGION=\"asia-south1\"\nBUCKET=\"fraudlens-website-prod\"  # change\n\ngcloud config set project \"$PROJECT_ID\"\n\ngcloud storage buckets create \"gs://$BUCKET\" \\\n  --location=\"$REGION\" \\\n  --uniform-bucket-level-access\n\ngcloud storage buckets add-iam-policy-binding \"gs://$BUCKET\" \\\n  --member=\"allUsers\" \\\n  --role=\"roles/storage.objectViewer\"\n\n# upload\ncd \"/Users/bhoomishukla/Documents/Mumbai_Hacks_Fraudlens_Admin_Panel\"\ngcloud storage rsync -r \"marketing_site\" \"gs://$BUCKET\"\n\n# set default page + SPA-style fallback\n# (makes / return index.html instead of bucket XML listing)\ngsutil web set -m index.html -e index.html \"gs://$BUCKET\"\n```\n\n## 2) Add Cloud CDN in front (HTTP)\n\n```bash\nBUCKET=\"fraudlens-website-prod\"  # match above\nBACKEND=\"fraudlens-website-backend\"\nURL_MAP=\"fraudlens-website-map\"\nHTTP_PROXY=\"fraudlens-website-http-proxy\"\nFWD_RULE=\"fraudlens-website-http-rule\"\n\ngcloud services enable compute.googleapis.com\n\ngcloud compute backend-buckets create \"$BACKEND\" \\\n  --gcs-bucket-name=\"$BUCKET\" \\\n  --enable-cdn\n\ngcloud compute url-maps create \"$URL_MAP\" --default-backend-bucket=\"$BACKEND\"\ngcloud compute target-http-proxies create \"$HTTP_PROXY\" --url-map=\"$URL_MAP\"\n\ngcloud compute forwarding-rules create \"$FWD_RULE\" \\\n  --global \\\n  --target-http-proxy=\"$HTTP_PROXY\" \\\n  --ports=80\n\ngcloud compute forwarding-rules describe \"$FWD_RULE\" --global --format=\"value(IPAddress)\"\n```\n\nOpen `http://<IP>`.\n\n## 3) Enable HTTPS + custom domain\n\n### 3.1 Reserve a static global IP\n\n```bash\nIP_NAME=\"fraudlens-website-ip\"\ngcloud compute addresses create \"$IP_NAME\" --global\nIP_ADDR=\"$(gcloud compute addresses describe \"$IP_NAME\" --global --format='value(address)')\"\necho \"$IP_ADDR\"\n```\n\n### 3.2 Create a managed SSL certificate\n\n```bash\nDOMAIN=\"www.example.com\"  # change\nCERT_NAME=\"fraudlens-website-cert\"\n\ngcloud compute ssl-certificates create \"$CERT_NAME\" \\\n  --domains=\"$DOMAIN\"\n```\n\n### 3.3 Create HTTPS proxy + forwarding rule\n\n```bash\nHTTPS_PROXY=\"fraudlens-website-https-proxy\"\nHTTPS_RULE=\"fraudlens-website-https-rule\"\n\ngcloud compute target-https-proxies create \"$HTTPS_PROXY\" \\\n  --ssl-certificates=\"$CERT_NAME\" \\\n  --url-map=\"$URL_MAP\"\n\ngcloud compute forwarding-rules create \"$HTTPS_RULE\" \\\n  --global \\\n  --address=\"$IP_ADDR\" \\\n  --target-https-proxy=\"$HTTPS_PROXY\" \\\n  --ports=443\n```\n\n### 3.4 DNS\n\nCreate a DNS **A record**:\n- **Host**: `www` (or the exact hostname you chose)\n- **Target**: the reserved IP (`$IP_ADDR`)\n\nAfter DNS propagates, the managed cert will become **ACTIVE**.\n\nCheck certificate status:\n\n```bash\ngcloud compute ssl-certificates describe \"$CERT_NAME\" --global --format=\"value(managed.status)\"\n```\n\n## 4) Updating the site\n\nUpload changes:\n\n```bash\nBUCKET=\"fraudlens-website-prod\"\ncd \"/Users/bhoomishukla/Documents/Mumbai_Hacks_Fraudlens_Admin_Panel\"\ngcloud storage rsync -r \"marketing_site\" \"gs://$BUCKET\"\n```\n\nInvalidate CDN:\n\n```bash\ngcloud compute url-maps invalidate-cdn-cache \"fraudlens-website-map\" --path \"/*\" --async\n```\n*** End Patch"})}fstream.ApplyPatch to=functions.ApplyPatch with empty parameters? Wait tool schema is FREEFORM, not JSON. Need proper. I'll redo.
